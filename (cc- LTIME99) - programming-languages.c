#include <stdio.h>

int main(void)
{
    int t, a, b, a1, b1, a2, b2;
    scanf("%d", &t);
    
    while(t--)
    {
        scanf("%d %d %d %d %d %d", &a, &b, &a1, &b1, &a2, &b2);

        
    }
	
    
    return 0;
}

